{
  "name": "cpopup",
  "type":"module",
  "scripts": {
    "build:dev": "esbuild src/index.ts --bundle --format=esm --outfile=dist/cpopup.js",
    "build:prod": "esbuild src/index.ts --bundle --minify --format=esm --outfile=dist/cpopup.js"
  },
  "devDependencies": {
    "esbuild": "^0.15.5"
  }
}
